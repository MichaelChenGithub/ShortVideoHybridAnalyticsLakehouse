# spark-defaults-aws.conf — Source of truth for EMR Serverless Spark configuration.
# Properties here are mirrored into the applicationConfiguration block in the
# Makefile submit-rt-* targets. Update both if you change a property.
# Local dev continues to use spark-defaults.conf (REST catalog + MinIO).
# Note: --properties-file cannot load from S3 at spark-submit startup time,
# so EMR Serverless jobs receive these via applicationConfiguration JSON instead.

# 1. Iceberg Spark Extensions (unchanged)
spark.sql.extensions                               org.apache.iceberg.spark.extensions.IcebergSparkSessionExtensions

# 2. Iceberg catalog — Glue Data Catalog replaces local REST catalog
spark.sql.catalog.lakehouse                        org.apache.iceberg.spark.SparkCatalog
spark.sql.catalog.lakehouse.catalog-impl           org.apache.iceberg.aws.glue.GlueCatalog
# Warehouse URL is injected at EMR job submission time via --conf override
# in airflow_batch_tasks.py (WAREHOUSE_BUCKET env var → spark.sql.catalog.lakehouse.warehouse).
# This placeholder is never used directly by EMR Serverless jobs.
spark.sql.catalog.lakehouse.warehouse              s3://warehouse/
spark.sql.catalog.lakehouse.io-impl                org.apache.iceberg.aws.s3.S3FileIO

# 3. S3 FileIO — IAM role-based auth; no endpoint override needed on AWS
spark.hadoop.fs.s3.impl                            org.apache.hadoop.fs.s3a.S3AFileSystem

# 4. Shuffle / memory (same as local)
spark.sql.shuffle.partitions                       8
spark.default.parallelism                          8
spark.driver.memory                                2g
spark.executor.memory                              2g
spark.memory.fraction                              0.6
spark.driver.memoryOverhead                        1g
spark.executor.memoryOverhead                      1g
spark.sql.files.maxPartitionBytes                  16777216
spark.sql.adaptive.enabled                         false

# 5. Packages — same runtime deps as local + MSK IAM auth for SASL/OAUTHBEARER
spark.jars.packages                                org.apache.iceberg:iceberg-spark-runtime-3.5_2.12:1.5.0,org.apache.hadoop:hadoop-aws:3.3.4,com.amazonaws:aws-java-sdk-bundle:1.12.262,org.apache.spark:spark-sql-kafka-0-10_2.12:3.5.1,software.amazon.msk:aws-msk-iam-auth:1.1.9
